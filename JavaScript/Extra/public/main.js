// 使用するDOM要素の取得今回は全てのDOMをどこでもいじれるようにグローバル変数にする
const word = document.getElementsByClassName('word');
const search = document.getElementsByClassName('search');
const photo = document.getElementsByClassName('photo-area');

